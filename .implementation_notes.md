# Nexo AI v3.1.0 — Implementation Notes

## Project: /home/ubuntu/nexo-app
- Next.js 14 (App Router), TypeScript, Tailwind CSS, Supabase, OpenRouter, Gemini
- Deployed at: nexo-app-delta.vercel.app
- GitHub: sumithk850-debug/Nexo-app

## Completed Features (v3.0.0 and v3.1.0)
1. AI Memory (Settings Panel → Long-term Memory)
2. Code Review Mode (Settings Panel → toggle)
3. User Feedback (👍👎 in MessageBubble → /api/feedback)
4. Web Search Grounding (Settings Panel → toggle, Gemini google_search tool)
5. Rate Limitation Panel (usage dashboard)
6. Announcement Modal (v3.1.0)
7. Image reading (nemotron-nano-12b-2-vl:free)

## Current Implementation (v3.2.0 — 5 new features)

### Files Created:
- components/SmartReplySuggestions.tsx — Quick reply buttons after assistant messages
- components/SessionResumeCard.tsx — "Welcome back" prompt with recent chats
- components/TypingSpeedIndicator.tsx — chars/sec badge during streaming

### Files Modified:
- components/MessageBubble.tsx:
  - Added `onSuggestionSelect` prop
  - Added SmartReplySuggestions render after last assistant message
  - Code block copy button already exists (lines 60-81)

- components/SettingsPanel.tsx:
  - Added search_grounding_enabled and code_review_enabled toggles

- components/LiveStatusBar.tsx:
  - NEEDS: Add typing speed badge display (TypingSpeedBadge component exists)

- components/ChatSidebar.tsx:
  - NEEDS: Add Chat Folder Organization (folders for grouping chats)

- app/page.tsx:
  - Added typing speed tracking (typingSpeedRef, typingSpeedTimerRef)
  - Added handleSuggestionSelect function
  - Added SessionResumeCard in render (when !activeChatId && chats.length > 0)
  - NEEDS: Pass typingSpeedRef.current to LiveStatusBar
  - NEEDS: Pass onSuggestionSelect to MessageBubble

### Progress Update:
✅ 1. Smart Reply Suggestions — DONE (component created, wired to MessageBubble in page.tsx)
✅ 2. Typing Speed Indicator — DONE (TypingSpeedBadge + TypingSpeedPill components, LiveStatusBar updated, page.tsx updated)
✅ 3. Session Resume Card — DONE (component created, rendered in page.tsx when no active chat)
⏳ 4. Code Snippet Copy Button — ALREADY EXISTS (MessageBubble CodeBlock has per-block copy button)
⏳ 5. Chat Folder Organization — TO DO (need to implement in ChatSidebar)

### Chat Folder Organization Plan:
- Use localStorage to store folder assignments (chatId → folderName)
- Default folders: "Today", "Yesterday", "This Week", "Older" (auto-categorized)
- Custom folders: user can create folders and drag chats into them
- Store in localStorage: nexo_chat_folders = { [chatId: string]: folderName }
- Render in ChatSidebar: group chats by folder with collapsible sections
- Keep existing rename/delete functionality intact
- Folders are frontend-only (no Supabase changes needed)

### Next Steps:
1. Implement Chat Folder Organization in ChatSidebar
2. Build and verify
3. Push to GitHub and deploy

### Supabase Schema:
- user_settings: session_id, memory_content, custom_persona, screen_share_enabled, search_grounding_enabled, code_review_enabled, response_length, language_preference, default_model
- feedback: id, session_id, message_id, model_id, rating, created_at
- announcements: id, version, title, message, is_active, created_at
- rate_limits: session_id, date, coder_query_count, message_count, last_reset
- chats: id, session_id, title, model_id, created_at, updated_at
- messages: id, chat_id, role, content, model_id, created_at

### Key Technical Details:
- ChatSidebar chats prop: DbChat[] from Supabase
- page.tsx MessageBubble render (around line 850-880): passes sessionId, isStreaming, onRegenerate, onEdit
- LiveStatusBar props: actions, streaming, repoFullName, searching
- LiveStatusBar renders above ChatInput in page.tsx render (around line 890)
- LiveStatusBar returns null if actions.length === 0 && !searching — need to add typing speed always-show option
